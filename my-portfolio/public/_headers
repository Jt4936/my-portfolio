# Hashed assets never change — cache them forever.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# Versioned 3D preview models and posters are static across a deployment.
/models/*
  Cache-Control: public, max-age=31536000, immutable

/model-posters/*
  Cache-Control: public, max-age=31536000, immutable

# HTML (index + all SPA routes) must always revalidate, so a new deploy
# is picked up immediately instead of serving a stale bundle reference.
/*
  Cache-Control: public, max-age=0, must-revalidate
